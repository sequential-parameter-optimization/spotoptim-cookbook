[project]
name = "bart25s"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.7",
    "pandas>=2.3.3",
    "spotoptim>=0.0.17",
]

[tool.uv.sources]
spotoptim = { path = "../spotoptim", editable = true }
